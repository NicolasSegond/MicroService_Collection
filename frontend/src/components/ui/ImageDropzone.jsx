import React from 'react';import { Upload, X, Image as ImageIcon } from 'lucide-react';const ImageDropzone = ({                           label,                           placeholder = "Ajouter une image",                           subtext = "Format supporté : JPG, PNG",                           icon = Upload,                           imagePreview,                           onFileSelect,                           onRemove,                           error,                           className = ""                       }) => {    const Icon = icon;    const handleZoneClick = () => {        document.getElementById('fileInput').click();    };    // Permet de déclencher l'upload avec la touche Entrée ou Espace    const handleKeyDown = (e) => {        if (e.key === 'Enter' || e.key === ' ') {            e.preventDefault();            handleZoneClick();        }    };    return (        <div className={`form-group ${className}`} style={{ height: '100%', display: 'flex', flexDirection: 'column' }}>            {label && (                <label style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '0.6rem', fontWeight: '600', color: '#333' }}>                    <ImageIcon size={16} />                    {label}                </label>            )}            <div                className={`image-upload-area ${imagePreview ? 'has-image' : ''}`}                onClick={handleZoneClick}                onKeyDown={handleKeyDown}                role="button"                tabIndex="0"                aria-label="Zone d'upload"                style={{                    borderColor: error ? '#d32f2f' : undefined,                    backgroundColor: error ? '#fff8f8' : undefined,                    cursor: 'pointer'                }}            >                <input                    id="fileInput"                    type="file"                    accept="image/*"                    onChange={onFileSelect}                    style={{ display: 'none' }}                />                {imagePreview ? (                    <div className="preview-container">                        <img src={imagePreview} alt="Preview" />                        <button                            type="button"                            className="remove-btn"                            onClick={(e) => {                                e.stopPropagation();                                onRemove();                            }}                            aria-label="Supprimer l'image"                            data-testid="remove-image-button"                        >                            <X size={18} />                        </button>                        <div className="overlay-edit">Cliquez pour changer</div>                    </div>                ) : (                    <div className="upload-placeholder">                        <div className="icon-circle">                            <Icon size={28} strokeWidth={1.5} />                        </div>                        <h3>{placeholder}</h3>                        <p>{subtext}</p>                    </div>                )}            </div>            {error && <span className="field-error" style={{color: '#d32f2f', fontSize: '0.85rem', marginTop: '0.25rem', display: 'block'}}>{error}</span>}        </div>    );};export default ImageDropzone;