apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: marketplace

resources:
  - namespace.yaml
  - infrastructure.yaml
  - auth.yaml
  - services.yaml
  - gateway.yaml
  # monitoring.yaml remplacé par kube-prometheus-stack (voir make monitoring-install)

labels:
  - pairs:
      app.kubernetes.io/part-of: marketplace
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false
