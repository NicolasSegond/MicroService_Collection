name: CI / CD Pipeline


on:
  push:
    branches: [ "main", "develop"]
  pull_request:
    branches: [ "main", "develop"]

env:
  NODE_VERSION: 22
  FRONT_COVERAGE: "frontend/coverage"


jobs:
  build-and-test:
    name: Build and Test Marketplace Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: ./frontend/package-lock.json

      - name: Install dependencies
        working-directory: ./frontend
        run: npm install

      - name: Run ESLint
        working-directory: ./frontend
        run: npm run lint

      - name: Tests unitaires + couverture (Vitest)
        working-directory: ./frontend
        run: npm run test -- --run --coverage --coverage.reporter=lcov

      - name: Artefact couverture frontend
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: ${{ env.FRONT_COVERAGE }}
          retention-days: 7

  sonarcloud:
    name: Qualité • SonarCloud (multi-langages)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build-and-test

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download frontend coverage artefact
        uses: actions/download-artifact@v4
        with:
          name: frontend-coverage
          path: ${{ env.FRONT_COVERAGE }}

      - name: Scan SonarCloud (PHP + JS/TS)
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=NicolasSegond_MicroService_Collection
            -Dsonar.organization=nicolassegond
            -Dsonar.sources=frontend/src
            -Dsonar.tests=frontend/src/__tests__,frontend/src/__mocks__
            -Dsonar.exclusions=**/vendor/**,**/var/**,**/node_modules/**,**/dist/**,**/__tests__/**,**/__mocks__/**
            -Dsonar.javascript.lcov.reportPaths=${{ env.FRONT_COVERAGE }}/lcov.info

  # ===========================================================================
  # ZAP SCAN - Local (CI with Docker Compose)
  # ===========================================================================
  zap-local:
    name: Security • ZAP Scan (Local)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'  # Only on PRs, not on deploy to staging

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Decrypt environment file
        env:
          ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
        run: |
          openssl enc -aes-256-cbc -d -pbkdf2 -in .env.enc -out .env -pass pass:"$ENCRYPTION_KEY"

      - name: Start services (CI mode - no auth)
        run: |
          docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d --build

      - name: Wait for services
        run: |
          echo "Waiting for Keycloak..."
          timeout 180 bash -c 'until curl -sf http://localhost:8080/health/ready; do sleep 5; done'
          
          # Wait for Traefik
          echo "Waiting for Traefik..."
          timeout 120 bash -c 'until docker compose exec -T traefik traefik healthcheck; do sleep 5; done'
          echo "Traefik is ready!"
          
          sleep 5
          echo "✅ All services ready!"

      - name: ZAP Baseline Scan
        run: |
          mkdir -p zap-reports
          chmod 777 zap-reports
          
          # Get the Docker network name created by docker-compose
          NETWORK_NAME=$(docker network ls --filter name=microservice -q | head -1)
          if [ -z "$NETWORK_NAME" ]; then
            NETWORK_NAME=$(docker network ls --format '{{.Name}}' | grep -E 'app-network|default' | head -1)
          fi
          echo "Using network: $NETWORK_NAME"
          
          # Run ZAP connected to the same network as the services
          # Use 'traefik' as hostname since that's the container name
          docker run --rm \
            --network "${NETWORK_NAME:-microservice_collection_app-network}" \
            -v ${{ github.workspace }}/.zap:/zap/wrk/.zap:ro \
            -v ${{ github.workspace }}/zap-reports:/zap/wrk/reports:rw \
            -t ghcr.io/zaproxy/zaproxy:stable \
            zap-baseline.py \
              -t http://traefik:80 \
              -J report_json.json \
              -w report_md.md \
              -r report_html.html \
              -a -j \
              -c /zap/wrk/.zap/rules.tsv \
            || echo "ZAP scan completed with findings"
          
          # Copy reports
          cp zap-reports/* ${{ github.workspace }}/ 2>/dev/null || true
          ls -la zap-reports/ || true

      - name: Upload ZAP Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-security-report
          path: |
            zap-reports/
            report_*.html
            report_*.json
            report_*.md
          if-no-files-found: warn

      - name: Stop services
        if: always()
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml down -v